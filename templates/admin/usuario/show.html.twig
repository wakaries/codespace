{% extends 'base.html.twig' %}

{% block title %}Usuario{% endblock %}

{% block body %}
    <h1>Usuario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ usuario.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ usuario.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ usuario.roles ? usuario.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ usuario.password }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ usuario.nombre }}</td>
            </tr>
            <tr>
                <th>Perfil</th>
                <td>{{ usuario.perfil }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_usuario_index') }}">back to list</a>

    <a href="{{ path('admin_usuario_edit', {'id': usuario.id}) }}">edit</a>

    {{ include('admin/usuario/_delete_form.html.twig') }}
{% endblock %}
