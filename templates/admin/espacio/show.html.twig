{% extends 'base.html.twig' %}

{% block title %}Espacio{% endblock %}

{% block body %}
    <h1>Espacio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ espacio.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ espacio.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_espacio_index') }}">back to list</a>

    <a href="{{ path('admin_espacio_edit', {'id': espacio.id}) }}">edit</a>

    {{ include('admin/espacio/_delete_form.html.twig') }}
{% endblock %}
